<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="DietPlanner.Mobile.Views.MealSearchPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:DietPlanner.Mobile.ViewModels"
    x:DataType="viewModels:MealSearchViewModel"
    Title="Meal search">
    <ContentPage.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles/MobileTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </ContentPage.Resources>
    <CollectionView BackgroundColor="{StaticResource PageBackgroundColor}" ItemsSource="{Binding Meals}" SelectionMode="None">
        <CollectionView.Header>
            <VerticalStackLayout Padding="20" Spacing="16">
                <Label Style="{StaticResource PageTitleStyle}" Text="{Binding ScreenTitle}" />
                <Border Style="{StaticResource CardBorderStyle}" Padding="16">
                    <VerticalStackLayout Spacing="6">
                        <Label Style="{StaticResource HeadlineBodyStyle}" Text="Current meal" />
                        <Label Style="{StaticResource SectionTitleStyle}" Text="{Binding CurrentMealName}" />
                    </VerticalStackLayout>
                </Border>
                <SearchBar Placeholder="Search meals" Text="{Binding SearchText}" />
                <Label TextColor="{StaticResource DangerColor}" Text="{Binding ErrorMessage}" />
            </VerticalStackLayout>
        </CollectionView.Header>
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="viewModels:MealSearchMealViewModel">
                <Border Style="{StaticResource CardBorderStyle}" Margin="20,0,20,12" Padding="16">
                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto" ColumnSpacing="10">
                        <Label FontAttributes="Bold" FontSize="17" Text="{Binding Name}" TextColor="{StaticResource PrimaryTextColor}" />
                        <Border
                            Grid.Row="1"
                            BackgroundColor="{StaticResource AccentSoftColor}"
                            HorizontalOptions="Start"
                            Padding="10,4"
                            StrokeThickness="0">
                            <Label Text="{Binding Type}" TextColor="{StaticResource AccentColor}" />
                        </Border>
                        <Label Grid.Row="2" Style="{StaticResource HeadlineBodyStyle}" Text="{Binding MacroSummary}" />
                        <Button
                            Grid.Column="1"
                            Grid.RowSpan="3"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ReplaceMealCommand}"
                            CommandParameter="{Binding .}"
                            Text="Use meal" />
                    </Grid>
                </Border>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
